
  # Location-based Pricing Display

  This is a code bundle for Location-based Pricing Display. The original project is available at https://www.figma.com/design/zV3eRMeFfMrGNBhZjv6dG0/Location-based-Pricing-Display.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  